"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
// Incorrect way to defining array 
var superMan = []; // []: means the type should always ber empty array but we are sending a string so it will throw an error
// superMan.push("hulk")
//Correct way of defining array
var superHeroes = []; // here this means it is a array of strings
superHeroes.push("spiderman");
var superPower = []; // here this means it is a array of number
superPower.push(1);
// Another way of defining array 
var supperMan1;
-type >= [];
var supperMan2 = [];
var supperMan3 = [];
var allUsers = [];
allUsers.push({
    name: "charu",
    age: 23
});
